{
    "name": "{{.DB_NAME}}{{.HASH}}",
    "deploy": "{{.DB_NAME}}{{.HASH}}",
    "ports": [{
        "name": "pg",
        "port": {{.DB_PORT}},
        "target_port": {{.DB_PORT}},
        "protocol": "TCP"
        }]
}