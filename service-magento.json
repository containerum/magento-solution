{
    "kind": "Service",
    "metadata": {
        "labels": {
            "external": "true",
            "{{.NS_SELECTOR}}": "{{.MAGENTO_NAME}}-{{.HASH}}"
        },
        "name": "{{.MAGENTO_NAME}}-{{.HASH}}"
    },
    "spec": {
        "selector": {
            "{{.NS_SELECTOR}}": "{{.MAGENTO_NAME}}-{{.HASH}}"
        },
        "ports": [
            {
                "protocol": "TCP",
                "targetPort": {{.MAGENTO_PORT}}
            }
        ]
    }
}
